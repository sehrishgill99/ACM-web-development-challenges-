// challenge 9 
const myObj = {
    prop1 : 'first value', 
    prop2 : 20 
};

 const myArray = [40, 50, 2];

 const result = myObj.prop2 === (myArray[0] / myArray[2]);


 // the result equal to true because myObj.prop2 equal t0 20 and (myArray[0] / myArray[2]) is ( 40 / 2) which is also equal to 20 therefore the answer is true 
 
